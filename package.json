{
    "name": "virtualdom-monorepo",
    "private": true,
    "scripts": {
        "build": "pnpm -r build",
        "dev": "pnpm --filter vite-app dev",
        "test": "pnpm -r test"
    },
    "devDependencies": {
        "typescript": "^5.0.0",
        "@types/node": "^18.0.0"
    }
}
